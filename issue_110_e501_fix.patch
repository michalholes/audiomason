diff --git a/src/audiomason/import_flow.py b/src/audiomason/import_flow.py
index 6c8c3e1..f1a2b45 100644
--- a/src/audiomason/import_flow.py
+++ b/src/audiomason/import_flow.py
@@ -839,7 +839,10 @@ def run_import(cfg: dict, src_path: Optional[Path] = None) -> None:
     if clean_inbox_mode not in {"ask", "yes", "no"}:
         die(f"Invalid configuration: clean_inbox must be one of ask|yes|no, got: {clean_inbox_mode!r}")
     if (state.OPTS and state.OPTS.yes) and clean_inbox_mode == "ask":
-        die("Non-interactive run requires an explicit inbox cleanup decision: set --clean-inbox yes|no (or config clean_inbox: yes|no)")
+        die(
+            "Non-interactive run requires an explicit inbox cleanup decision: "
+            "set --clean-inbox yes|no (or config clean_inbox: yes|no)"
+        )

@@ -1206,7 +1209,11 @@ def run_import(cfg: dict, src_path: Optional[Path] = None) -> None:
                     if getattr(state, "DEBUG", False):
                         out(
-                            f"[ol] author result: ok={getattr(ar, 'ok', None)} status={getattr(ar, 'status', None)!r} hits={getattr(ar, 'hits', None)} top={getattr(ar, 'top', None)!r}"
+                            f"[ol] author result: ok={getattr(ar, 'ok', None)} "
+                            f"status={getattr(ar, 'status', None)!r} "
+                            f"hits={getattr(ar, 'hits', None)} "
+                            f"top={getattr(ar, 'top', None)!r}"
                         )

@@ -1247,7 +1254,11 @@ def run_import(cfg: dict, src_path: Optional[Path] = None) -> None:
                         if getattr(state, "DEBUG", False):
                             out(
-                                f"[ol] book result: ok={getattr(br, 'ok', None)} status={getattr(br, 'status', None)!r} hits={getattr(br, 'hits', None)} top={getattr(br, 'top', None)!r}"
+                                f"[ol] book result: ok={getattr(br, 'ok', None)} "
+                                f"status={getattr(br, 'status', None)!r} "
+                                f"hits={getattr(br, 'hits', None)} "
+                                f"top={getattr(br, 'top', None)!r}"
                             )
